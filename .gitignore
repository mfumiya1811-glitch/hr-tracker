# 秘密キー。絶対にコミットしないこと
.env

# 作業用の一時ファイル（DL済みPDF・抽出テキスト）
.cache/

# 注意：config.js は意図的に公開しています。
# 中身は Supabase の「公開用キー（sb_publishable_）」であり、
# RLS によりログインしない限り何も読めないため、公開して問題ありません。

# 削除した企業のバックアップ（人事データを含むため公開しない）
backup/
