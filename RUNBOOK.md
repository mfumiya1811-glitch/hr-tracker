# 巡回（クローリング）手順書

月1回の運用手順です。人事プレスは各社「年数回」程度なので、**月1回で十分**です。

---

## 毎月やること（所要 10〜20分）

### 1. 巡回を依頼する

このフォルダで Claude Code を開き、こう言うだけです。

> 5社を巡回して、前回以降の新着を pending に入れて

私が以下を自動で行います。

1. 各社のプレス一覧を開く
2. 企業マスタの判定ルールで人事記事を絞り込む
3. `articles` テーブルのURLと突合して**既読分を除外**
4. 本文（またはPDF）から異動情報を抽出
5. 出典URL・原文抜粋・確信度を付けて `pending` に投入
6. `last_crawled_at` と `crawl_runs` を更新
7. 結果を報告

### 2. レビューする

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File start.ps1
```

`http://localhost:4321` が開きます。公開URL <https://mfumiya1811-glitch.github.io/hr-tracker/> でも同じです。

「レビュー待ち」タブで1件ずつ確認します。

- **引用ブロック（原文抜粋）を読む**
- **「出典を開いて確認 ↗」で原本と突き合わせる**
- 合っていれば「承認して一覧へ」、違えば「却下」
- 入力欄は編集可能。直してから承認できます

**`確信度 medium` / `low` を優先的に見てください。** そこが誤りやすい箇所です。

### 3. 完了

承認したものだけが「異動一覧」に載ります。CSV / JSON で出力できます。

---

## 対象企業と、各社の癖

| 企業 | 形式 | 判定方法 | 注意点 |
|---|---|---|---|
| グラクソ・スミスクライン | HTML | URL `/YYYYMMDD-hr/` | 規則が完璧。**取締役級のみ**公開 |
| 第一三共 | **PDF** | リンク文言 | 人事プレスが最も多い。PDFはURLから判別不能 |
| 中外製薬 | HTML | リンク文言 | 表形式。役員・参与・基本組織長を公開 |
| アムジェン | HTML | リンク文言 | **日本語プレスが極端に少なく人事記事ゼロ**。役員一覧の差分頼み |
| 旭化成セラピューティクス | **PDF** | リンク文言 | **親会社のグループ全体プレスを見る**（下記） |

### 旭化成セラピューティクスの特殊事情

自社サイトには人事情報が載りません。**親会社の旭化成グループ全体プレス**を見ます。

- 一覧: `https://www.asahi-kasei.com/jp/news/YYYY`
- PDF: `https://www.asahi-kasei.com/content/dam/asahi-kasei/jp/ja/news/YYYY/pdf/ze{YYMMDD}.pdf`
- グループ全社分が1本のPDFにまとまっているため、**「旭化成ファーマ株式会社」の節だけを抽出**する
- 親会社側の上席理事一覧にも「［旭化成ファーマ 執行役員］」として転出が載るので併せて確認
- 2026-04-01 に旭化成ファーマから社名変更
- タイトルが年により揺れる：`組織改正および人事異動について` / `〜役員等の異動について` / `〜役員等の体制について`

発表実績：2024-02-08 / 2024-09-24 / 2024-10-18 / 2025-01-06 / 2026-02-05

---

## 技術的な決まりごと

### PDF の読み方

```powershell
& "C:\Program Files\Git\mingw64\bin\pdftotext.exe" -enc UTF-8 -table 入力.pdf 出力.txt
```

**必ず `-table` を使う。** `-layout` だと表の行がばらけて氏名と役職の対応が崩れます。第一三共で実際に踏みました。

### データの入れ方（守ること）

- **発表日** = プレスリリースの掲載日。**異動日**（原文の「〜付」）とは別物
- **部署名は推測で埋めない**。原文にない場合は空欄のまま
- 「退任」「辞任」は原文で使い分けられている。両方拾い、原文表記を備考に残す
- `source_url` と `source_quote` は**必ず埋める**。承認時の裏取りに使う
- 確信度は正直につける。表の折り返しで対応が怪しい、日付が原文にない等は `medium` / `low`

### 企業の追加・削除

```powershell
# 追加（robots.txt を自動判定し、AIを拒否しているサイトは blocked にする）
powershell -File add-company.ps1 -Name "社名" -PressUrl "https://..." -Format html

# 巡回対象から外す（データは残る）
powershell -File remove-company.ps1 -Name "社名" -Disable

# 完全に削除（人事データも消える／戻せない）
powershell -File remove-company.ps1 -Name "社名" -Purge
```

`-Purge` は件数を表示したうえで企業名の入力を求めます。**削除前にバックアップを取ること。**

### 接続確認

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File test-connection.ps1
```

テーブルの有無・企業数・件数・書き込み権限を確認します。キーは表示されません。

---

## 使わないと決めたソース

| ソース | 理由 |
|---|---|
| ミクスOnline | robots.txt が ClaudeBot / anthropic-ai を明示的に拒否 |
| LinkedIn | robots.txt が自動アクセスを明示的に禁止。加えて個人情報 |
| 日刊薬業 | 冒頭のみ無料で以降は会員限定（有料購読） |
| 官報 | 株式会社の役員変更は商業登記簿であり官報には載らない |
| 武田薬品工業 | 一覧が JavaScript 描画で取得できず（robots.txt 上は許可） |
| アステラス製薬 | 人事記事の所在が未特定 |

新しい企業を追加するときは、`add-company.ps1` が robots.txt を自動判定します。

---

## トラブル時

| 症状 | 対処 |
|---|---|
| ログインできない | メールのリンクは1回限り。**メール内の6桁の数字**でも入れる。標準メール機能は1時間に数通の上限あり |
| `start.ps1` が起動しない | ポート4321が使用中。黒い画面が既に開いていないか確認（ポート3000はWindowsが予約済みで使えない） |
| 画面にデータが出ない | ログインしていない。RLSにより未ログインでは1件も読めない仕様 |
| スクリプトが文字化け | `.ps1` は BOM 付き UTF-8 で保存する（PowerShell 5.1 の制約） |

---

## 残っている宿題

- 第一三共の「取締役候補者」8件が 2026-06-22 株主総会で承認されたかの裏取り（`medium` のまま）
- 第一三共 2026-02-24「4月1日付 人事異動」の部長級が未取得
- 旭化成の監査役 大野勝則の異動日が「6月定時株主総会以降」としか書かれておらず `2026-06-30` で仮置き（`low`）

---

## 自動実行（設定済み）

**毎月1日 午前9時**に自動で巡回するタスクを登録してあります。

- タスク名：`hr-tracker-monthly-crawl`（サイドバーの「Scheduled」に表示）
- 完了時に通知が届きます
- 実行内容はこの手順書と同じ。巡回して `pending` に投入するところまでで、**承認は自動化していません**

**注意：このアプリが開いているときに動きます。** 1日にアプリが閉じていた場合は、次に起動したタイミングで実行されます。

止めたい・時刻を変えたいときは「そのタスクを止めて」「毎月15日に変えて」と伝えれば変更します。

手動で今すぐ回したいときは、これまでどおり次のように頼んでください。

> 5社を巡回して、前回以降の新着を pending に入れて
